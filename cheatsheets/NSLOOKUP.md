# Nslookup cheatsheet

```sh
nslookup

# Commands to run once the console is running
set type=a
set type=mx
set type=cname
set type=aaaa

server 9.9.9.9 # set the DNS here

google.com # then type the name of the domain you want information from
```