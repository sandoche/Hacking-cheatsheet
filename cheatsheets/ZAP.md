# OWASP Zed Attack Proxy (ZAP) cheatsheet

```sh
# Set up proxy firefox port 8080 (for passive scan)
# Need to generate a certificate and add it to the browser as an authority

# Run spidering to the website
# Run active scan
```